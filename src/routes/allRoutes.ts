import { routes as contentListRoutes } from 'modules/MovieList/routes';
import { routes as dashboardRoutes } from 'modules/Dashboard/routes';
import { routes as watchlistRoutes } from 'modules/Watchlist/routes';

export const allRoutes = [contentListRoutes, dashboardRoutes, watchlistRoutes];
